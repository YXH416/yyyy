# 本项目交付约定

- 修改直接提交并推送本仓库；提交消息标注修改编号，不建立历史ROUND差异目录。
- 用户不需要生成可烧录文件。默认只做源码语法/逻辑验证，不运行固件链接或输出.out/.hex/.bin。
- 每次修改后运行tools/update_copy_bundle.ps1，刷新工作区根目录固定的“待复制文件”文件夹，供用户按原目录复制。
- 每轮先更新tools/copy_manifest.json中的本轮必要文件；复制包不得含固件、目标文件、整个Debug或测试程序。
- 实测参数必须来自用户的真实日志，不把示例值当作测量值。
